# Mini web app for marking visited places

## For launching the app

First of all add images folder to back folder. 
Have docker, in terminal write while being in the root folder docker compose up --build. 
AFter successfull launch, you have post front url written in a terminal to a web browser 
