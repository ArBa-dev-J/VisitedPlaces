info:
  name: updateCityName
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:3000/api/v1/cities/:id/updateCity
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
