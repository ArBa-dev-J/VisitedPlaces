info:
  name: uploadNewPlace
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/v1/places/newPlace
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
