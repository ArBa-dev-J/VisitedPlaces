info:
  name: getAllCities
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/api/v1/cities/cityList?name=Belgium
  params:
    - name: name
      value: Belgium
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
