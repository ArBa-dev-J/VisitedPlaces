info:
  name: addNewCity
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/v1/cities/newCity
  body:
    type: json
    data: |
      {
        "name": "-800"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
